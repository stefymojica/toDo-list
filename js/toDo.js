const writeNote = document.getElementById('writeNote');
const addNewNote = document.getElementById('addNewNote');
const noteList = document.getElementsByClassName("noteList");

addNewNote.addEventListener("click", () => {
    
})


